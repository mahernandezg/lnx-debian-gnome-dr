# Restore Guide

Restoration is selective rather than blind.

Each recovery area must support:

- Inspection
- Dry-run
- Explicit confirmation
- Execution
- Verification

The restore process must never overwrite sensitive files silently.

A full restoration should be tested first in a compatible Debian virtual
machine before being used after a real hardware failure.
